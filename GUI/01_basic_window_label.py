import tkinter as tk

# TODO: create the main window (replace None with tk.Tk())
root = None

# TODO: create a Label that displays "Gomycode" (replace None with tk.Label(...))
lbl = None

# TODO: pack the label into the window
# (your code here)

if root is not None:
    root.mainloop()
